# Hugo configuration file
title: WhyPhi

# import hextra as module
module:
  imports:
  - path: github.com/imfing/hextra

markup:
  # allow raw html
  goldmark:
    renderer:
      unsafe: true

  # enable hextra syntax highlight
  highlight:
    noClasses: false

menu:
  main:
    - name: Onboarding
      pageRef: "/onboarding"
      weight: 1
    - name: Usage
      pageRef: "/usage"
      weight: 2
    - name: Docs
      pageRef: /docs
      weight: 3
    - name: Workflow
      pageRef: /workflow
      weight: 4
    - name: Blog
      pageRef: "/blog"
      weight: 5
    - name: Search
      weight: 6
      params:
        type: search
    - name: GitHub
      weight: 7
      url: "https://github.com/whyphi"
      params:
        icon: github

params:
  navbar:
    displayTitle: true
    displayLogo: false

  footer:
    displayCopyright: false
    displayPoweredBy: false

  editURL:
    enable: true
    base: "https://github.com/whyphi/whyphi.github.io/edit/main/content"
